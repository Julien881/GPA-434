#include <print>
#include "StringImage.h"

int main() 
{
	StringImage im;
	im.initialise (5, 4, '#');
	//im.setPixel(2, 1, '#');

	char v;
	//v = im.setPixel(3, 3);

	//std::print("{}", im.toString())

	return 0;
}